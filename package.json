{
  "name": "cloner",
  "version": "1.0.0",
  "main": "./bin/index.js",
  "license": "MIT",
  "preferGlobal": true,
  "types": "./bin/index.d.ts",
  "bin": {
    "super-cloner": "./bin/index.js"
  },
  "dependencies": {
    "@types/commander": "^2.12.2",
    "@types/inquirer": "^6.5.0",
    "axios": "^0.19.2",
    "chalk": "^3.0.0",
    "commander": "^4.1.0",
    "dotenv": "^8.2.0",
    "inquirer": "^7.0.3",
    "rxjs": "^6.5.4"
  },
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "clean": "rm -rf bin",
    "build": "tsc ",
    "start": "yarn clean && yarn build && node ./bin/index.js",
    "prepublishOnly": "yarn clean && yarn build"
  },
  "devDependencies": {
    "typescript": "^3.7.5"
  }
}
