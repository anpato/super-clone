import * as dotenv from 'dotenv'
dotenv.config()

export const GITHUB_USERNAME: undefined | string = process.env.GITHUB_USERNAME
export const GITHUB_TOKEN: undefined | string = process.env.GITHUB_TOKEN
